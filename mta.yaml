_schema-version: "3.2"
ID: invoice
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: invoice-destination-content
  type: com.sap.application.content
  requires:
  - name: invoice-destination-service
    parameters:
      content-target: true
  - name: invoice-repo-host
    parameters:
      service-key:
        name: invoice-repo-host-key
  - name: invoice-uaa
    parameters:
      service-key:
        name: invoice-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: invoice_html_repo_host
          ServiceInstanceName: invoice-html5-service
          ServiceKeyName: invoice-repo-host-key
          sap.cloud.service: invoice
        - Authentication: OAuth2UserTokenExchange
          Name: invoice_uaa
          ServiceInstanceName: invoice-xsuaa-service
          ServiceKeyName: invoice-uaa-key
          sap.cloud.service: invoice
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: invoice-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: invoice-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - invoice.zip
      name: invoice
      target-path: resources/
- name: invoice
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: invoice-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: invoice-destination-service
    service-plan: lite
- name: invoice-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: invoice-xsuaa-service
    service-plan: application
- name: invoice-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: invoice-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
